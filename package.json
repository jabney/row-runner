{
  "name": "row-runner",
  "version": "0.1.0",
  "description": "Process csv files as streams of rows",
  "main": "dist/index.js",
  "scripts": {
    "build": "npm run clean && tsc",
    "clean": "rm -rf dist",
    "prepare": "npm run build",
    "test": "npm run build && tap --ts --no-check-coverage",
    "test-ts": "npm run clean && tap --ts --no-check-coverage"
  },
  "author": "James Abney",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.0.3",
    "@types/tap": "^15.0.7",
    "tap": "^16.3.0",
    "ts-node": "^10.8.1",
    "typescript": "^4.7.4"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "seedable": "^1.0.5"
  }
}